---
phase: 01-ai-plant-identification
plan: 03
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified:
  - src/components/ui/aiResultCard/AiResultCard.tsx
  - src/components/ui/aiResultCard/AiResultCard.styles.ts
autonomous: true
requirements:
  - AI-02
  - AI-04

must_haves:
  truths:
    - AI identification results display with plant name, description, care instructions
    - Confidence badge visible alongside results
    - User can edit AI results before saving (inline editing)
    - Editable fields match plant form requirements
  artifacts:
    - path: src/components/ui/aiResultCard/AiResultCard.tsx
      provides: AI result display + inline editing component
      exports: AiResultCard({ result, onEdit, onConfirm })
  key_links:
    - from: AiResultCard.tsx
      to: ConfidenceBadge
      via: imports and displays confidence badge
      pattern: <ConfidenceBadge confidence={result.probability} />
    - from: AiResultCard.tsx
      to: plantService.ts
      via: onConfirm passes edited data to addPlant
      pattern: onConfirm({ nombre, categoria, descripcion, cuidados, ... })
---

<objective>
Create AI results display card with inline editing capability. Shows plant identification results and allows user to edit before saving.

**Requirements addressed:**
- AI-02: Display IA results (name, description, care)
- AI-04: Accept or edit before saving
</objective>

<context>
@.planning/phases/01-ai-plant-identification/01-CONTEXT.md
@.planning/phases/01-ai-plant-identification/01-RESEARCH.md
@src/components/ui/confidenceBadge/ConfidenceBadge.tsx
@src/components/ui/appInput/AppInput.tsx
@src/components/ui/wateringFrequencyPicker/WateringFrequencyPicker.tsx

**Editable fields from CONTEXT.md D-03:**
- Nombre, Categoría, Descripción, Cuidados, Frecuencia de riego
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AiResultCard component</name>
  <files>
    - src/components/ui/aiResultCard/AiResultCard.tsx
    - src/components/ui/aiResultCard/AiResultCard.styles.ts
  </files>
  <read_first>
src/components/ui/confidenceBadge/ConfidenceBadge.tsx
src/components/ui/appInput/AppInput.tsx
src/theme/desingSystem.ts
  </read_first>
  <action>
Create the following files:

**1. AiResultCard.styles.ts**
```typescript
import { StyleSheet } from "react-native";
import { useTheme } from "../../../theme/desingSystem";

export const useAiResultCardStyles = () => {
  const theme = useTheme();
  
  return StyleSheet.create({
    container: {
      backgroundColor: theme.colors.surface,
      borderRadius: theme.radius.xl,
      padding: 20,
      marginVertical: 8,
    },
    header: {
      flexDirection: "row",
      justifyContent: "space-between",
      alignItems: "flex-start",
      marginBottom: 16,
    },
    plantName: {
      fontSize: 24,
      fontFamily: theme.typography.fontFamily.bold,
      color: theme.colors.textPrimary,
      flex: 1,
    },
    latinName: {
      fontSize: 14,
      fontFamily: theme.typography.fontFamily.italic,
      color: theme.colors.textSecondary,
      marginTop: 4,
    },
    section: {
      marginBottom: 16,
    },
    sectionTitle: {
      fontSize: 12,
      fontFamily: theme.typography.fontFamily.semibold,
      color: theme.colors.textSecondary,
      textTransform: "uppercase",
      marginBottom: 6,
    },
    sectionContent: {
      fontSize: 15,
      fontFamily: theme.typography.fontFamily.regular,
      color: theme.colors.textPrimary,
      lineHeight: 22,
    },
    editModeContainer: {
      gap: 12,
    },
    buttonRow: {
      flexDirection: "row",
      gap: 12,
      marginTop: 20,
    },
    editButton: {
      flex: 1,
      padding: 16,
      borderRadius: theme.radius.xl,
      backgroundColor: theme.colors.surfaceElevated,
      borderWidth: 1,
      borderColor: theme.colors.border,
      alignItems: "center",
    },
    confirmButton: {
      flex: 1,
      padding: 16,
      borderRadius: theme.radius.xl,
      backgroundColor: theme.colors.primary,
      alignItems: "center",
    },
    buttonText: {
      fontSize: 16,
      fontFamily: theme.typography.fontFamily.bold,
    },
  });
};
```

**2. AiResultCard.tsx**
```typescript
import React, { useState } from "react";
import { View, Text, TextInput, TouchableOpacity, ScrollView } from "react-native";
import { Ionicons } from "@expo/vector-icons";
import { useTheme } from "../../../theme/desingSystem";
import { PlantIdentificationResult } from "../../../types-dtos/plant.types";
import { useAiResultCardStyles } from "./AiResultCard.styles";
import ConfidenceBadge from "../confidenceBadge/ConfidenceBadge";
import AppInput from "../appInput/AppInput";

interface AiResultCardProps {
  result: PlantIdentificationResult;
  onEdit: (editedData: PlantEditData) => void;
  onConfirm: (confirmedData: PlantEditData) => void;
}

export interface PlantEditData {
  nombre: string;
  categoria: string;
  descripcion: string;
  cuidados: string;
  frecuenciaRiego: string;
  confidence: number;
}

export default function AiResultCard({ result, onEdit, onConfirm }: AiResultCardProps) {
  const styles = useAiResultCardStyles();
  const theme = useTheme();
  const [isEditing, setIsEditing] = useState(false);
  const [editData, setEditData] = useState<PlantEditData>({
    nombre: result.plantName,
    categoria: "",
    descripcion: result.description || result.wikiDescription?.extract || "",
    cuidados: result.careInstructions || "",
    frecuenciaRiego: "7",
    confidence: result.probability,
  });

  const handleConfirm = () => {
    onConfirm(editData);
  };

  const handleEdit = () => {
    setIsEditing(true);
    onEdit(editData);
  };

  if (isEditing) {
    // Edit mode - inline form
    return (
      <View style={styles.container}>
        <Text style={[styles.sectionTitle, { marginBottom: 16 }]}>
          Editar información de la planta
        </Text>
        
        <View style={styles.editModeContainer}>
          <AppInput
            label="Nombre"
            value={editData.nombre}
            onChangeText={(text) => setEditData({ ...editData, nombre: text })}
            placeholder="Nombre de la planta"
          />
          
          <AppInput
            label="Categoría"
            value={editData.categoria}
            onChangeText={(text) => setEditData({ ...editData, categoria: text })}
            placeholder="Ej: Interior, Exterior, Suculenta..."
          />
          
          <AppInput
            label="Descripción"
            value={editData.descripcion}
            onChangeText={(text) => setEditData({ ...editData, descripcion: text })}
            placeholder="Descripción de la planta"
            multiline
            numberOfLines={3}
          />
          
          <AppInput
            label="Cuidados"
            value={editData.cuidados}
            onChangeText={(text) => setEditData({ ...editData, cuidados: text })}
            placeholder="Instrucciones de cuidado"
            multiline
            numberOfLines={3}
          />
        </View>
        
        <View style={styles.buttonRow}>
          <TouchableOpacity 
            style={styles.editButton}
            onPress={() => setIsEditing(false)}
          >
            <Text style={[styles.buttonText, { color: theme.colors.textPrimary }]}>
              Cancelar
            </Text>
          </TouchableOpacity>
          
          <TouchableOpacity 
            style={styles.confirmButton}
            onPress={handleConfirm}
          >
            <Text style={[styles.buttonText, { color: theme.colors.textOnAccent }]}>
              Guardar
            </Text>
          </TouchableOpacity>
        </View>
      </View>
    );
  }

  // Display mode
  return (
    <View style={styles.container}>
      <View style={styles.header}>
        <View style={{ flex: 1 }}>
          <Text style={styles.plantName}>{result.plantName}</Text>
          {result.latinName && (
            <Text style={styles.latinName}>{result.latinName}</Text>
          )}
        </View>
        <ConfidenceBadge confidence={result.probability} size="large" />
      </View>
      
      {result.description && (
        <View style={styles.section}>
          <Text style={styles.sectionTitle}>Descripción</Text>
          <Text style={styles.sectionContent}>{result.description}</Text>
        </View>
      )}
      
      {result.careInstructions && (
        <View style={styles.section}>
          <Text style={styles.sectionTitle}>Cuidados</Text>
          <Text style={styles.sectionContent}>{result.careInstructions}</Text>
        </View>
      )}
      
      <View style={styles.buttonRow}>
        <TouchableOpacity 
          style={styles.editButton}
          onPress={handleEdit}
        >
          <Ionicons name="create-outline" size={20} color={theme.colors.textPrimary} />
          <Text style={[styles.buttonText, { color: theme.colors.textPrimary, marginLeft: 8 }]}>
            Editar
          </Text>
        </TouchableOpacity>
        
        <TouchableOpacity 
          style={styles.confirmButton}
          onPress={handleConfirm}
        >
          <Ionicons name="checkmark-circle" size={20} color={theme.colors.textOnAccent} />
          <Text style={[styles.buttonText, { color: theme.colors.textOnAccent, marginLeft: 8 }]}>
            Confirmar
          </Text>
        </TouchableOpacity>
      </View>
    </View>
  );
}
```
  </action>
  <verify>
    <automated>ls src/components/ui/aiResultCard/</automated>
  </verify>
  <done>
    - Displays plant name, latin name, confidence badge
    - Shows description and care instructions from AI
    - Edit mode allows modifying all fields before save
    - Uses existing AppInput component for consistency
  </done>
</task>

</tasks>

<verification>
- Component compiles without errors
- Edit mode shows form fields
- Confirm mode shows results with actions
</verification>

<success_criteria>
- AI results visible with confidence badge
- User can tap "Editar" to modify fields
- User can tap "Confirmar" to proceed with save
</success_criteria>

<output>
After completion, create `.planning/phases/01-ai-plant-identification/01-03-PLAN-SUMMARY.md`
</output>