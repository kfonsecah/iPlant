---
phase: 01-ai-plant-identification
plan: 05
type: execute
wave: 4
depends_on: [01-04]
files_modified:
  - app.json
  - .env.example
autonomous: true
requirements:
  - AI-01

must_haves:
  truths:
    - API key is configured via environment variable (never committed)
    - App.json has placeholder for API key with instructions
    - Example env file documents required configuration
  artifacts:
    - path: app.json
      provides: EXPO_PUBLIC_PLANT_ID_API_KEY configuration
    - path: .env.example
      provides: Template for required environment variables
  key_links:
    - from: app.json
      to: plantService.ts
      via: reads EXPO_PUBLIC_PLANT_ID_API_KEY
      pattern: process.env.EXPO_PUBLIC_PLANT_ID_API_KEY
---

<objective>
Configure environment variables for Plant.id API key. Ensure the key is never committed to the repository.

**Requirements addressed:**
- AI-01: Photo submission to IA API (configuration aspect)
</objective>

<context>
@.planning/phases/01-ai-plant-identification/01-CONTEXT.md
@src/services/plantService.ts
@app.json

**Security from RESEARCH.md and CONTEXT.md:**
- API key handled via environment variable (never committed)
- Use EXPO_PUBLIC_ prefix for client-side access
</context>

<tasks>

<parameter name="Task 1: Configure API key in app.json</name>
  <files>app.json</files>
  <read_first>app.json</read_first>
  <action>
Add the Plant.id API key configuration to the extra section of app.json. Read app.json first to find the correct location.

After reading, add/update:

```json
{
  "expo": {
    "extra": {
      "plantIdApiKey": "YOUR_PLANT_ID_API_KEY_HERE"
    }
  }
}
```

Or if there's already an "extra" section, add the key there.
  </action>
  <verify>
    <automated>grep -q "plantIdApiKey\|PLANT_ID" app.json</automated>
  </verify>
  <done>
    - app.json has plantIdApiKey in extra section
    - Placeholder value indicates where to add real key
  </done>
</task>

<task type="auto">
  <name>Task 2: Create .env.example template</name>
  <files>.env.example</files>
  <action>
Create .env.example in the project root with the following content:

```bash
# Plant.id API Key (get free key at https://web.plant.id/)
# Register for free account, get API key from dashboard
EXPO_PUBLIC_PLANT_ID_API_KEY=your_api_key_here

# Optional: Other environment variables
# FIREBASE_API_KEY=your_firebase_key
# FIREBASE_PROJECT_ID=your_project_id
```

This file documents required environment variables without exposing actual keys.
  </action>
  <verify>
    <automated>ls .env.example</automated>
  </verify>
  <done>
    - .env.example exists with Plant.id API key placeholder
    - Instructions for getting free API key included
  </done>
</task>

<task type="auto">
  <name>Task 3: Update plantService to use app.json config</name>
  <files>src/services/plantService.ts</files>
  <read_first>src/services/plantService.ts</read_first>
  <action>
Update the plantService.ts to read the API key from app.json config as fallback:

Replace the API key constant definition:

```typescript
// Plant.id API configuration
const PLANT_ID_API_URL = "https://api.plant.id/v3/identification";
// Try environment variable first, then app.json config, then throw error
const PLANT_ID_API_KEY = 
  process.env.EXPO_PUBLIC_PLANT_ID_API_KEY || 
  (require('../../app.json').expo?.extra?.plantIdApiKey) ||
  "";
```

This provides a fallback for development without requiring .env setup.
  </action>
  <verify>
  <automated>grep -q "app.json" src/services/plantService.ts</automated>
  </verify>
  <done>
    - plantService reads API key from env var or app.json config
    - Clear error message if key is not configured
  </done>
</task>

</tasks>

<verification>
- No actual API keys in codebase
- Configuration supports both .env and app.json approaches
</verification>

<success_criteria>
- Developer can configure API key via app.json or .env
- Service throws clear error if API key is missing
- No secrets committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/01-ai-plant-identification/01-05-PLAN-SUMMARY.md`
</output>