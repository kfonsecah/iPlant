---
phase: 03-offline-mode-local-storage
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [src/services/syncService.ts, src/services/plantService.ts, src/types-dtos/plant.types.ts, src/services/__tests__/syncService.test.ts, src/services/__tests__/plantService.test.ts]
autonomous: true
requirements: [OFFL-02, OFFL-03, STOR-02, STOR-03]

must_haves:
  truths:
    - "SyncService manages a persistent FIFO queue of pending actions"
    - "PlantService implements the Repository pattern (Local Cache first)"
    - "New plants are added to the local cache and sync queue when offline"
    - "List fetching returns cached data when offline"
  artifacts:
    - path: "src/services/syncService.ts"
      provides: "Queue persistence and processing"
    - path: "src/services/plantService.ts"
      provides: "Cache-aware data operations"
  key_links:
    - from: "src/services/plantService.ts"
      to: "src/services/storageService.ts"
      via: "Direct calls for cache and image persistence"
    - from: "src/services/plantService.ts"
      to: "src/services/syncService.ts"
      via: "Enqueuing write operations"
---

<objective>
Implement the core offline logic by refactoring the plant service into a Repository pattern and establishing a persistent synchronization queue.

Purpose: To allow the app to function without immediate network access by redirecting reads to a local cache and buffering writes in a queue.
Output: Refactored plantService.ts and new syncService.ts.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Moviles/iPlant/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrador/Desktop/U-Local/Moviles/iPlant/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-offline-mode-local-storage/03-CONTEXT.md
@.planning/phases/03-offline-mode-local-storage/03-RESEARCH.md
@.planning/phases/03-offline-mode-local-storage/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Sync Service</name>
  <files>src/services/syncService.ts, src/types-dtos/plant.types.ts</files>
  <action>
    1. Update `src/types-dtos/plant.types.ts` to include `isPending?: boolean` and `syncError?: string` in `PlantaCompletaInterface`.
    2. Create `src/services/syncService.ts`:
       - Define `SyncAction` interface: `{ id: string, type: 'CREATE', data: any, timestamp: number }`.
       - Implement `addToQueue(action: SyncAction)`: Saves to AsyncStorage key `SYNC_QUEUE_${userId}`.
       - Implement `getQueue()`: Retrieves the current list.
       - Implement `removeFromQueue(id: string)`.
       - Implement `processQueue()`:
         - Sequential loop (Sequential Sync Logic from RESEARCH.md).
         - For each item, call the appropriate API service.
         - On success, remove from queue and update local cache `isPending` status.
         - Use a lock/flag to prevent concurrent sync processes.
  </action>
  <verify>
    <automated>ls src/services/syncService.ts</automated>
  </verify>
  <done>SyncService manages a persistent queue of pending write operations.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Plant Service (Repository Pattern)</name>
  <files>src/services/plantService.ts</files>
  <action>
    Refactor `src/services/plantService.ts` to use "Repository with Local-First" pattern.
    
    1. **getPlantsByUserId:**
       - If online: Fetch from Firestore, update local cache, return data.
       - If offline: Return data from local cache via `storageService`.
    2. **addPlant:**
       - Generate local UUID using `expo-crypto`.
       - Persist image via `storageService.persistImage`.
       - Create plant object with `isPending: true`.
       - Save to local cache.
       - Add to `syncService` queue.
       - Return the local object immediately (optimistic UI).
    3. **identifyPlant:**
       - Wrap with connectivity check: throw error if offline (per CONTEXT.md D-01).
  </action>
  <verify>
    <automated>grep "isPending" src/services/plantService.ts</automated>
  </verify>
  <done>PlantService supports local-first operations and connectivity-aware identification.</done>
</task>

<task type="auto">
  <name>Task 3: Unit Testing for Services</name>
  <files>src/services/__tests__/syncService.test.ts, src/services/__tests__/plantService.test.ts</files>
  <action>
    1. Create unit tests for `syncService.ts`:
       - Test adding to queue.
       - Test retrieving queue.
       - Test clearing items.
    2. Create unit tests for `plantService.ts` (using mocks for Firestore and Storage):
       - Test offline read returns cached data.
       - Test offline write creates pending item and enqueues.
  </action>
  <verify>
    <automated>npm test src/services/__tests__/</automated>
  </verify>
  <done>Services validated with automated unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SyncService → API | Data sent during sync must be validated on the backend (existing Firestore rules/logic). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Denial of Service | SyncService | mitigate | Implement sequential processing and error backoff to prevent overwhelming the API or device during sync. |
</threat_model>

<verification>
1. Verify `syncService.ts` exists and handles queue persistence.
2. Verify `plantService.ts` calls `storageService` for caching.
3. Verify tests pass.
</verification>

<success_criteria>
- Plants are saved locally when offline.
- Pending items are correctly enqueued.
- App loads cached plants when network is unavailable.
- Sync process can be triggered and sequentially calls the API.
</success_criteria>

<output>
After completion, create `.planning/phases/03-offline-mode-local-storage/03-02-SUMMARY.md`
</output>
