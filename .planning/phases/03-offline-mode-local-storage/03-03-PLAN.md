---
phase: 03-offline-mode-local-storage
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: [src/screens/misPlants/MisPlants.tsx, src/screens/camera/CameraScreen.tsx, src/components/ui/offlineBanner/OfflineBanner.tsx, src/services/syncService.ts]
autonomous: true
requirements: [OFFL-04, OFFL-05]

must_haves:
  truths:
    - "Plants pending sync display a distinct visual indicator in the list"
    - "Identification flow shows error/warning when offline"
    - "Manual sync button triggers queue processing with feedback"
    - "Offline items are correctly uploaded to Firestore upon sync"
  artifacts:
    - path: "src/screens/misPlants/MisPlants.tsx"
      provides: "List with sync indicators"
    - path: "src/screens/camera/CameraScreen.tsx"
      provides: "Offline-aware identification UI"
  key_links:
    - from: "src/components/ui/offlineBanner/OfflineBanner.tsx"
      to: "src/services/syncService.ts"
      via: "Sync Now button call"
---

<objective>
Complete the offline experience by integrating sync indicators into the UI and providing a manual synchronization mechanism.

Purpose: To provide transparency to the user about their data's sync status and allow them to control when to upload pending items.
Output: Updated screens and components with sync feedback and triggers.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Moviles/iPlant/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrador/Desktop/U-Local/Moviles/iPlant/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-offline-mode-local-storage/03-CONTEXT.md
@.planning/phases/03-offline-mode-local-storage/03-RESEARCH.md
@.planning/phases/03-offline-mode-local-storage/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update MisPlants UI for Sync Status</name>
  <files>src/screens/misPlants/MisPlants.tsx</files>
  <action>
    1. Update the plant card rendering in `MisPlants.tsx`.
    2. Add a conditional check for `item.isPending`.
    3. If `isPending` is true, render a visual indicator:
       - Icon: `Ionicons` "cloud-upload-outline" or "time-outline".
       - Label: "Pendiente" (small text, low opacity).
       - Style: Use `nativewind` for subtle styling (e.g., grayscale or dashed border).
  </action>
  <verify>
    <automated>grep "isPending" src/screens/misPlants/MisPlants.tsx</automated>
  </verify>
  <done>MisPlants screen displays visual indicators for plants pending synchronization.</done>
</task>

<task type="auto">
  <name>Task 2: Update Identification Flow (Offline Handling)</name>
  <files>src/screens/camera/CameraScreen.tsx</files>
  <action>
    1. Integrate `useConnectivity` into `CameraScreen.tsx`.
    2. Before calling `identifyPlant`, check `isConnected`.
    3. If offline:
       - Prevent the call to the AI service.
       - Show a Toast or Modal informing: "Modo offline. La identificación por IA requiere internet."
       - Allow the user to "Save anyway" (manual entry/placeholder) if the design allows, or simply disable the "Identify" button.
    4. Ensure that if identification was done online but "Save" happens offline, the plant is correctly enqueued (handled by service, but UI must reflect this).
  </action>
  <verify>
    <automated>grep "isConnected" src/screens/camera/CameraScreen.tsx</automated>
  </verify>
  <done>Camera/Identification flow handles offline state gracefully per user decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Manual Sync Integration</name>
  <files>src/components/ui/offlineBanner/OfflineBanner.tsx, src/services/syncService.ts</files>
  <action>
    1. Add a "Sincronizar ahora" button to `OfflineBanner.tsx`.
    2. On press, call `syncService.processQueue()`.
    3. Use a loading state (spinner) in the banner while syncing.
    4. Show feedback upon completion (Toast or Banner message change).
    5. Ensure `MisPlants` refreshes after a successful sync to clear indicators.
  </action>
  <verify>
    <automated>grep "processQueue" src/components/ui/offlineBanner/OfflineBanner.tsx</automated>
  </verify>
  <done>Manual synchronization is functional and provides feedback to the user.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Sync Button | The user initiates the sync, providing a clear trust boundary for data transmission. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Information Disclosure | UI Indicators | accept | The indicator only shows to the authenticated owner of the data. |
</threat_model>

<verification>
1. Verify pending indicators show in `MisPlants` when data is queued.
2. Verify "Sync Now" button triggers the sync process.
3. Verify items move from "Pending" to "Synced" state after connection returns and sync is triggered.
</verification>

<success_criteria>
- End-to-end offline flow works: Add plant offline -> See "Pending" -> Click "Sync Now" (with internet) -> Plant appears in Firestore and indicator disappears.
</success_criteria>

<output>
After completion, create `.planning/phases/03-offline-mode-local-storage/03-03-SUMMARY.md`
</output>
