---
phase: 02-camera-permission-handling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/screens/camera/CameraScreen.tsx]
autonomous: true
requirements: [PERM-01, PERM-02, PERM-03, PERM-04]

must_haves:
  truths:
    - "El usuario ve un botón para abrir ajustes si denegó el permiso permanentemente"
    - "El usuario puede seleccionar una foto de la galería incluso si no dio permiso de cámara"
    - "La app detecta automáticamente el cambio de permiso al volver de los ajustes del sistema"
  artifacts:
    - path: "src/screens/camera/CameraScreen.tsx"
      provides: "Enhanced permission guard with settings redirection and gallery fallback"
  key_links:
    - from: "src/screens/camera/CameraScreen.tsx"
      to: "Linking.openSettings"
      via: "onPress handler"
    - from: "src/screens/camera/CameraScreen.tsx"
      to: "ImagePicker.launchImageLibraryAsync"
      via: "Gallery button"
---

<objective>
Implementar un manejo robusto de permisos de cámara que guíe al usuario en caso de denegación, permita el uso de la galería como alternativa y facilite la recuperación del permiso mediante la redirección a los ajustes del sistema.

Salida: Pantalla de cámara actualizada con guardián de permisos inteligente.
</objective>

<execution_context>
@C:/Users/Administrador/Desktop/U-Local/Moviles/iPlant/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrador/Desktop/U-Local/Moviles/iPlant/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-camera-permission-handling/02-CONTEXT.md
@.planning/phases/02-camera-permission-handling/02-RESEARCH.md
@src/screens/camera/CameraScreen.tsx
@src/theme/desingSystem.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementar Guardián de Permisos Mejorado</name>
  <files>src/screens/camera/CameraScreen.tsx</files>
  <read_first>src/screens/camera/CameraScreen.tsx</read_first>
  <action>
    Actualizar el bloque de renderizado `if (!permission.granted)` en `CameraScreen.tsx` para implementar las decisiones D-01, D-02 y D-03:
    
    1. Importar `Linking` de `react-native`.
    2. Dentro del bloque de denegación de permisos:
       - Detectar denegación permanente: `const isPermanentlyDenied = !permission.canAskAgain;`.
       - Si es permanente, mostrar botón "Abrir Ajustes" que ejecute `Linking.openSettings()`. (cumple PERM-04)
       - Si no es permanente, mantener botón "Activar Cámara" que ejecute `requestPermission()`. (cumple PERM-03)
       - Añadir un botón secundario "Seleccionar de Galería" que llame a la función `pickImage` existente. (cumple D-02)
    3. Asegurar que el mensaje explicativo sea claro y directo según D-04.
    4. Aplicar estilos consistentes con el sistema de diseño usando `theme.colors.primary` para el botón principal y `theme.colors.secondary` o un estilo de borde para el de galería.
  </action>
  <verify>
    <automated>grep -E "Linking\.openSettings|canAskAgain|pickImage" src/screens/camera/CameraScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - El archivo contiene lógica para `permission.canAskAgain`.
    - Se llama a `Linking.openSettings()` en el flujo de denegación permanente.
    - El botón de galería está presente dentro del bloque `!permission.granted`.
  </acceptance_criteria>
  <done>La UI de permisos ahora ofrece opciones dinámicas basadas en el estado del sistema y una vía alternativa vía galería.</done>
</task>

<task type="auto">
  <name>Task 2: Añadir Refresco Automático de Permisos</name>
  <files>src/screens/camera/CameraScreen.tsx</files>
  <read_first>src/screens/camera/CameraScreen.tsx</read_first>
  <action>
    Implementar un listener de `AppState` para que la app re-valide los permisos automáticamente cuando el usuario regrese de los ajustes del sistema:
    
    1. Importar `AppState` y `useEffect` (si no están).
    2. En el componente `CameraScreen`, añadir un `useEffect`:
       ```typescript
       useEffect(() => {
         const subscription = AppState.addEventListener('change', nextAppState => {
           if (nextAppState === 'active') {
             getPermission(); // Función devuelta por useCameraPermissions
           }
         });
         return () => subscription.remove();
       }, []);
       ```
    3. Asegurarse de extraer `getPermission` del hook: `const [permission, requestPermission, getPermission] = useCameraPermissions();`.
  </action>
  <verify>
    <automated>grep -E "AppState\.addEventListener|getPermission\(\)" src/screens/camera/CameraScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Se utiliza `AppState` para detectar cuando la app vuelve al primer plano.
    - Se invoca `getPermission()` para actualizar el estado del hook `useCameraPermissions`.
  </acceptance_criteria>
  <done>La app se sincroniza con el estado de permisos del sistema sin requerir reinicio o navegación manual.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Validación Manual del Flujo de Permisos</name>
  <what-built>Flujo completo de permisos con redirección a ajustes, galería fallback y refresco automático.</what-built>
  <how-to-verify>
    1. Ejecutar la app en un emulador o dispositivo real.
    2. Navegar a la pantalla de Cámara.
    3. **Escenario A (Denegación Temporal):** Denegar el permiso una vez. Verificar que aparece "Activar Cámara".
    4. **Escenario B (Denegación Permanente):** Denegar de nuevo (o marcar "No volver a preguntar"). Verificar que el botón cambia a "Abrir Ajustes".
    5. **Escenario C (Redirección):** Pulsar "Abrir Ajustes", habilitar la cámara en el sistema y volver a la app. La cámara debería activarse automáticamente.
    6. **Escenario D (Galería):** Con el permiso de cámara denegado, pulsar "Seleccionar de Galería". Verificar que se abre el selector de imágenes y la identificación funciona.
  </how-to-verify>
  <resume-signal>Aprobado tras verificar los 4 escenarios.</resume-signal>
  <done>El usuario ha confirmado que el manejo de errores y permisos es fluido y cumple con los requerimientos PERM-01 a PERM-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → System Settings | Trusting the OS to report correct permission status via Linking and AppState |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Camera Access | Mitigate | Expo Camera permissions are managed at the OS level. We ensure we only request what's needed. |
| T-02-02 | Denial of Service | App Crash on Deny | Mitigate | Use of `permission.granted` guard ensures we never initialize CameraView without permission. |
</threat_model>

<verification>
- PERM-01: No crashes on denial (verified by code structure).
- PERM-02: Message clarity (UI review).
- PERM-03: Re-request works (Manual test).
- PERM-04: Settings redirect works (Manual test).
</verification>

<success_criteria>
- La pantalla de cámara no intenta renderizar el `CameraView` si `permission.granted` es falso.
- El botón de galería permite identificar plantas sin acceso a la cámara.
- La transición de "Denegado" a "Permitido" vía Ajustes es automática al re-enfocar la app.
</success_criteria>

<output>
After completion, create `.planning/phases/02-camera-permission-handling/02-01-PLAN-SUMMARY.md`
</output>
