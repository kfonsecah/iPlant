import { AppTheme } from "../../theme/desingSystem";

export const createMisPlantasStyles = (theme: AppTheme) => ({
  safeArea: {
    flex: 1,
    backgroundColor: theme.colors.background,
  },
  container: {
    flex: 1,
    backgroundColor: theme.colors.background,
  },
  bgImage: {
    opacity: theme.mode === "dark" ? 0.04 : 0.08,
    tintColor: theme.colors.primary,
  },
  header: {
    flexDirection: "row" as const,
    alignItems: "center" as const,
    justifyContent: "space-between" as const,
    backgroundColor: theme.colors.backgroundCard,
    borderRadius: 24,
    paddingHorizontal: 18,
    paddingVertical: 10,
    marginHorizontal: 16,
    marginTop: 12,
    marginBottom: 4,
    borderWidth: 1,
    borderColor: theme.colors.border,
    ...theme.shadows,
  },
  headerTitle: {
    color: theme.colors.textPrimary,
    fontFamily: theme.typography.fontFamily.bold,
    fontSize: 22,
    fontWeight: "300" as const,
    letterSpacing: -0.5,
  },
  filterBtn: {
    width: 38,
    height: 38,
    borderRadius: 12,
    alignItems: "center" as const,
    justifyContent: "center" as const,
    backgroundColor: theme.colors.backgroundCard,
    borderWidth: 1,
    borderColor: theme.colors.border,
  },
  searchBar: {
    flexDirection: "row" as const,
    alignItems: "center" as const,
    backgroundColor: theme.colors.backgroundCard,
    borderRadius: 16,
    paddingHorizontal: 14,
    height: 46,
    marginHorizontal: 16,
    marginTop: 12,
    borderWidth: 1,
    borderColor: theme.colors.border,
    gap: 10,
    ...theme.shadows,
  },
  searchInput: {
    flex: 1,
    fontSize: 13,
    color: theme.colors.textPrimary,
    padding: 0,
  },
  scrollView: {
    flex: 1,
  },
  scrollContent: {
    paddingHorizontal: 0,
    paddingTop: 0,
    paddingBottom: 130, // Account for the floating tab bar height at the bottom
  },
  summaryCard: {
    flexDirection: "row" as const,
    gap: 10,
    marginBottom: 20,
  },
  summaryItem: {
    flex: 1,
    alignItems: "center" as const,
    backgroundColor: theme.mode === "dark" ? "rgba(74, 222, 128, 0.04)" : "rgba(74, 222, 128, 0.08)",
    borderRadius: 20,
    paddingVertical: 12,
    paddingHorizontal: 8,
    borderWidth: 1,
    borderColor: theme.mode === "dark" ? "rgba(74, 222, 128, 0.15)" : "rgba(74, 222, 128, 0.3)",
    gap: 2,
  },
  summaryValue: {
    color: theme.colors.primary,
    fontFamily: theme.typography.fontFamily.bold,
    fontSize: 22,
    fontWeight: "700" as const,
  },
  summaryLabel: {
    color: theme.colors.disabledText,
    fontFamily: theme.typography.fontFamily.regular,
    fontSize: 11,
  },
  sectionTitle: {
    color: theme.colors.textPrimary,
    fontFamily: theme.typography.fontFamily.bold,
    fontSize: 16,
    fontWeight: "600" as const,
    letterSpacing: 0.2,
  },
  sectionRow: {
    flexDirection: "row" as const,
    alignItems: "center" as const,
    marginBottom: 14,
    marginTop: 10,
  },
  sectionAccentBar: {
    width: 3,
    height: 16,
    borderRadius: 2,
    backgroundColor: theme.colors.primary,
    marginRight: 8,
  },
  grid: {
    flexDirection: "row" as const,
    flexWrap: "wrap" as const,
    gap: 12,
    marginBottom: 14,
  },
  plantCard: {
    width: "48%" as const,
    aspectRatio: 0.72,
    borderRadius: 24,
    backgroundColor: theme.colors.backgroundCard,
    borderWidth: 1,
    borderColor: theme.colors.border,
    padding: 8,
    ...theme.shadows,
  },
  imageContainer: {
    width: "100%" as const,
    height: "65%" as const,
    borderRadius: 18,
    overflow: "hidden" as const,
    position: "relative" as const,
  },
  plantImage: {
    width: "100%" as const,
    height: "100%" as const,
  },
  plantChip: {
    position: "absolute" as const,
    top: 6,
    left: 6,
    paddingHorizontal: 8,
    paddingVertical: 3,
    backgroundColor: theme.mode === "dark" ? "rgba(0, 0, 0, 0.65)" : "rgba(255, 255, 255, 0.85)",
    borderRadius: 10,
    borderWidth: 1,
    borderColor: theme.mode === "dark" ? "rgba(74, 222, 128, 0.2)" : "rgba(74, 222, 128, 0.4)",
    flexDirection: "row" as const,
    alignItems: "center" as const,
  },
  plantChipText: {
    color: theme.colors.primary,
    fontFamily: theme.typography.fontFamily.semibold,
    fontSize: 9,
    fontWeight: "600" as const,
    letterSpacing: 0.2,
  },
  plantInfoContainer: {
    paddingHorizontal: 4,
    paddingVertical: 8,
    flex: 1,
    justifyContent: "center" as const,
  },
  plantName: {
    color: theme.colors.textPrimary,
    fontFamily: theme.typography.fontFamily.bold,
    fontSize: 14,
    fontWeight: "700" as const,
    marginBottom: 4,
  },
  waterRow: {
    flexDirection: "row" as const,
    alignItems: "center" as const,
    gap: 6,
  },
  waterText: {
    color: theme.colors.primary,
    fontFamily: theme.typography.fontFamily.semibold,
    fontSize: 11,
    fontWeight: "600" as const,
  },

  // ── Pending sync ─────────────────────────────────────────────────────────────
  pendingBadge: {
    position: "absolute" as const,
    top: 6,
    right: 6,
    backgroundColor: theme.mode === "dark" ? "rgba(0, 0, 0, 0.65)" : "rgba(255, 255, 255, 0.85)",
    paddingHorizontal: 8,
    paddingVertical: 3,
    borderRadius: 10,
    flexDirection: "row" as const,
    alignItems: "center" as const,
    gap: 4,
    borderWidth: 1,
    borderColor: theme.colors.border,
  },
  pendingText: {
    color: theme.colors.textPrimary,
    fontFamily: theme.typography.fontFamily.semibold,
    fontSize: 9,
    fontWeight: "600" as const,
    textTransform: "uppercase" as const,
  },

  // ── Health & alert ──────────────────────────────────────────────────────────────────
  healthDot: {
    width: 6,
    height: 6,
    borderRadius: 3,
  },
  alertBanner: {
    backgroundColor: theme.mode === "dark" ? "rgba(245, 158, 11, 0.04)" : "rgba(251, 191, 36, 0.08)",
    borderRadius: 20,
    borderWidth: 1,
    borderColor: theme.mode === "dark" ? "rgba(245, 158, 11, 0.18)" : "rgba(251, 191, 36, 0.3)",
    padding: 14,
    marginBottom: 14,
    gap: 8,
  },
  alertHeader: {
    flexDirection: "row" as const,
    alignItems: "center" as const,
    gap: 6,
    marginBottom: 2,
  },
  alertTitle: {
    color: theme.colors.warning,
    fontFamily: theme.typography.fontFamily.semibold,
    fontSize: 13,
    fontWeight: "600" as const,
  },
  alertItem: {
    flexDirection: "row" as const,
    alignItems: "center" as const,
    gap: 8,
  },
  alertPlantName: {
    flex: 1,
    color: theme.colors.textPrimary,
    fontFamily: theme.typography.fontFamily.regular,
    fontSize: 13,
  },
  alertWaterText: {
    color: theme.colors.warning,
    fontFamily: theme.typography.fontFamily.semibold,
    fontSize: 11,
    fontWeight: "600" as const,
  },

  // ── FAB ─────────────────────────────────────────────────────────────────────
  fab: {
    position: "absolute" as const,
    bottom: 120, // Floats cleanly right above the floating glassy tab bar on all devices
    right: 20,
    width: 54,
    height: 54,
    borderRadius: 27,
    backgroundColor: theme.colors.primary,
    alignItems: "center" as const,
    justifyContent: "center" as const,
    shadowColor: theme.colors.primary,
    shadowOpacity: 0.35,
    shadowRadius: 10,
    shadowOffset: { width: 0, height: 4 },
    elevation: 8,
  },

  // ── Modal ────────────────────────────────────────────────────────────────────
  modalOverlay: {
    flex: 1,
    backgroundColor: "rgba(0, 0, 0, 0.8)",
    justifyContent: "flex-end" as const,
  },
  modalCard: {
    backgroundColor: theme.colors.backgroundCard,
    borderTopLeftRadius: 28,
    borderTopRightRadius: 28,
    borderWidth: 1,
    borderColor: theme.colors.border,
    paddingHorizontal: 20,
    paddingBottom: 30,
    paddingTop: 12,
    gap: 16,
  },
  modalHandle: {
    width: 36,
    height: 4,
    borderRadius: 2,
    backgroundColor: theme.colors.border,
    alignSelf: "center" as const,
    marginBottom: 4,
  },
  modalHeader: {
    flexDirection: "row" as const,
    alignItems: "center" as const,
    justifyContent: "space-between" as const,
  },
  modalTitle: {
    fontFamily: theme.typography.fontFamily.bold,
    fontSize: 18,
    fontWeight: "700" as const,
    color: theme.colors.textPrimary,
  },
  modalCloseBtn: {
    width: 32,
    height: 32,
    borderRadius: 10,
    backgroundColor: theme.colors.backgroundCard,
    borderWidth: 1,
    borderColor: theme.colors.border,
    alignItems: "center" as const,
    justifyContent: "center" as const,
  },

  // ── Inputs dentro del modal ──────────────────────────────────────────────────
  fieldLabel: {
    fontFamily: theme.typography.fontFamily.semibold,
    fontSize: 13,
    fontWeight: "600" as const,
    color: theme.colors.textSecondary,
    marginBottom: 6,
  },
  textInput: {
    height: 48,
    borderRadius: 12,
    borderWidth: 1,
    borderColor: theme.colors.inputBorder,
    backgroundColor: theme.colors.inputBackground,
    paddingHorizontal: 16,
    fontFamily: theme.typography.fontFamily.regular,
    fontSize: 13,
    color: theme.colors.textPrimary,
  },
  textInputError: {
    borderColor: theme.colors.error,
  },
  fieldError: {
    fontFamily: theme.typography.fontFamily.regular,
    fontSize: 11,
    color: theme.colors.error,
    marginTop: 4,
  },

  // ── Chips de categoría ───────────────────────────────────────────────────────
  chipsRow: {
    flexDirection: "row" as const,
    flexWrap: "wrap" as const,
    gap: 8,
  },
  chip: {
    paddingHorizontal: 12,
    paddingVertical: 6,
    borderRadius: 14,
    borderWidth: 1,
    borderColor: theme.colors.border,
    backgroundColor: theme.colors.backgroundCard,
  },
  chipSelected: {
    backgroundColor: theme.mode === "dark" ? "rgba(74, 222, 128, 0.08)" : "rgba(74, 222, 128, 0.12)",
    borderColor: theme.colors.primary,
  },
  chipText: {
    fontFamily: theme.typography.fontFamily.medium,
    fontSize: 12,
    fontWeight: "500" as const,
    color: theme.colors.textSecondary,
  },
  chipTextSelected: {
    color: theme.colors.primary,
  },

  // ── Botón guardar ────────────────────────────────────────────────────────────
  saveButton: {
    height: 48,
    borderRadius: 14,
    backgroundColor: theme.colors.primary,
    alignItems: "center" as const,
    justifyContent: "center" as const,
    marginTop: 10,
  },
  saveButtonText: {
    fontFamily: theme.typography.fontFamily.semibold,
    fontSize: 14,
    fontWeight: "700" as const,
    color: theme.colors.textOnAccent,
  },
  // ── Empty State ─────────────────────────────────────────────────────────────
  emptyContainer: {
    flex: 1,
    alignItems: "center" as const,
    justifyContent: "center" as const,
    paddingVertical: 60,
    width: "100%" as const,
    gap: 12,
  },
  emptyText: {
    fontFamily: theme.typography.fontFamily.medium,
    fontSize: 13,
    textAlign: "center" as const,
    color: theme.colors.textSecondary,
  },
});
